<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/bluetooth_search"
        android:title="@string/search_devices"
        android:icon="@drawable/ic_bluetooth_device_add"
        app:showAsAction="always"/>
    <item android:id="@+id/bluetooth_enable"
        android:title="@string/enable_bluetooth"
        android:icon="@drawable/ic_bluetooth_enable"
        app:showAsAction="always"/>
</menu>